<%= render "devise/shared/error_messages", resource: resource %>
<div class="form-forgotPassword w-100 m-auto">
<h4 class="my-3 text-center">ユーザ名の確認と<br>パスワードの再発行</h4>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <div class="form-floating pb-2">
      <%= f.email_field :email, class: "form-control", id: "floatingEmail", placeholder: "Email", type: "email" %>
      <label for="floatingEmail">メールアドレス</label>
    </div>
    <div class="actions">
      <%= f.submit "送信", class: "btn btn-primary w-100 py-2" %>
    </div>
  <% end %>
  <hr>
  <div class="link py-1">
    <%= link_to new_session_path(resource_name), class: "link", data: {"turbolinks" => false} do %>
      <i class="bi bi-lightbulb"></i>&ensp;ユーザー名・パスワードを思い出した方はこちら
    <% end %>
  </div>
  <div class="link py-1">
    <%= link_to new_registration_path(resource_name), class: "link", data: {"turbolinks" => false} do %>
      <i class="bi bi-person-circle"></i>&ensp;新規登録はこちら
    <% end %>
  </div>
</div>
